/api/auth/register:
  post:
    tags: [Auth]
    summary: Register a new user
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RegisterInput"
    responses:
      "201":
        $ref: "#/components/responses/RegisterResponse"
      "400":
        description: Validation error
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
            example:
              success: false
              message: Validation failed
              error:
                code: VALIDATION_ERROR
                errors:
                  - field: email
                    message: Invalid email format
                  - field: password
                    message: Must be at least 8 characters
      "409":
        description: Email or username already exists
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
            examples:
              EmailAlreadyRegistered:
                value:
                  success: false
                  message: Email already registered
                  error:
                    code: CONFLICT_ERROR
              UsernameAlreadyTaken:
                value:
                  success: false
                  message: Username already taken
                  error:
                    code: CONFLICT_ERROR

/api/auth/verify-email:
  post:
    tags: [Auth]
    summary: Verify email with OTP
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/VerifyEmailInput"
    responses:
      "201":
        $ref: "#/components/responses/VerifyEmailResponse"
      "400":
        description: Validation error or invalid verification code
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
      "404":
        description: User not found
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"

/api/auth/verify-otp:
  post:
    tags: [Auth]
    summary: Verify OTP and issue temporary reset token
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/VerifyOtpInput"
    responses:
      "200":
        $ref: "#/components/responses/VerifyOtpResponse"
      "400":
        description: Validation error or invalid verification code
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
      "404":
        description: User not found
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"

/api/auth/resend-verification:
  post:
    tags: [Auth]
    summary: Resend email verification OTP
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ResendVerificationOTPInput"
    responses:
      "200":
        $ref: "#/components/responses/ResendVerificationResponse"
      "400":
        description: Validation error
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
      "403":
        description: Account suspended
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
      "404":
        description: User not found
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
      "409":
        description: Email already verified
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"

/api/auth/refresh:
  post:
    tags: [Auth]
    summary: Refresh access token
    description: Rotates access and refresh tokens using refresh token from cookie.
    parameters:
      - in: cookie
        name: refreshToken
        required: true
        schema:
          type: string
        description: Refresh token stored in HTTP-only cookie.
    responses:
      "200":
        $ref: "#/components/responses/RefreshTokenResponse"
      "400":
        description: Missing refresh token
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
            example:
              success: false
              message: Refresh token is required
              error:
                code: MISSING_TOKEN
      "401":
        description: Invalid or revoked refresh token
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"

/api/auth/logout:
  post:
    tags: [Auth]
    summary: Logout user
    description: Revokes refresh token when present and clears auth cookies.
    parameters:
      - in: cookie
        name: refreshToken
        required: false
        schema:
          type: string
        description: Optional refresh token cookie.
    responses:
      "200":
        $ref: "#/components/responses/LogoutResponse"

/api/auth/login:
  post:
    tags: [Auth]
    summary: Login user
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/LoginInput"
    responses:
      "200":
        $ref: "#/components/responses/LoginResponse"
      "400":
        description: Validation error
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
            example:
              success: false
              message: Validation failed
              error:
                code: VALIDATION_ERROR
                errors:
                  - field: email
                    message: Required
                  - field: password
                    message: Must be at least 8 characters
      "401":
        description: Invalid credentials
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
            examples:
              InvalidCredentials:
                value:
                  success: false
                  message: Invalid email or password
                  error:
                    code: AUTHENTICATION_ERROR
              AccountInactive:
                value:
                  success: false
                  message: Account is inactive
                  error:
                    code: AUTHENTICATION_ERROR
      "403":
        description: Access denied
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
            examples:
              EmailNotVerified:
                value:
                  success: false
                  message: Email not verified. A new verification code has been sent.
                  error:
                    code: EMAIL_NOT_VERIFIED
              AccountPending:
                value:
                  success: false
                  message: Your business account is pending admin approval.
                  error:
                    code: ACCOUNT_PENDING
              AccountSuspended:
                value:
                  success: false
                  message: Your business account has been suspended.
                  error:
                    code: ACCOUNT_SUSPENDED

/api/auth/forgot-password:
  post:
    tags: [Auth]
    summary: Request password reset OTP
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ForgotPasswordInput"
    responses:
      "200":
        $ref: "#/components/responses/ForgotPasswordResponse"
      "400":
        description: Validation error
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErrorResponse"
            example:
              success: false
              message: Validation failed
              error:
                code: VALIDATION_ERROR
                errors:
                  - field: email
                    message: Required

/api/auth/reset-password:
  post:
    tags: [Auth]
    summary: Reset password using OTP
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ResetPasswordInput"
    responses:
      "200":
        $ref: "#/components/responses/ResetPasswordResponse"
      "400":
        description: Invalid or expired OTP

/api/auth/me:
  get:
    tags: [Auth]
    summary: Get current user profile
    security:
      - bearerAuth: []
    responses:
      "200":
        $ref: "#/components/responses/UserProfileResponse"
      "401":
        description: Unauthorized
