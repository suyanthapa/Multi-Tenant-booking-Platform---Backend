meta {
  name: Verify-Email
  type: http
  seq: 3
}

post {
  url: http://localhost:3001/api/auth/verify-email
  body: json
  auth: inherit
}

body:json {
  {
    "token": "john"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
