meta {
  name: Reject Business
  type: http
  seq: 12
}

patch {
  url: http://localhost:8000/api/businesses/biz_seed_004/reject
  body: json
  auth: inherit
}

body:json {
  {
      "rejectionReasons": [
     
      "UNRESPONSIVE"
    ],
    "adminNote": "Please upload valid registration certificate and complete your profile."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
